<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<form id="delete-form-{{ commande.id }}" method="post" action="{{ path('app_commande_delete', {'id': commande.id}) }}" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button type="button" class="btn btn-mauve btn-sm" onclick="confirmDeleteCommande({{ commande.id }})">
        <i class="fas fa-trash-alt"></i> Supprimer
    </button>
</form>
<script>
    function confirmDeleteCommande(commandeId) {
        Swal.fire({
            title: "Êtes-vous sûr ?",
            text: "Cette action est irréversible !",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#800080",
            cancelButtonColor: "#3085d6",
            confirmButtonText: "Oui, supprimer",
            cancelButtonText: "Annuler"
        }).then((result) => {
            if (result.isConfirmed) {
                document.getElementById("delete-form-" + commandeId).submit();
            }
        });
    }
</script>
<style>
.btn-mauve {
    background-color: #6f42c1; /* Mauve */
    color: white;
    border: none;
    padding: 6px 12px;
    border-radius: 5px;
    font-size: 14px;
    transition: 0.3s ease-in-out;
    margin: 2px;
    display: inline-block;
}

.btn-mauve:hover {
    background-color: #5a32a3; /* Mauve plus foncé */
}

</style>