{% extends 'base.html.twig' %}

{% block title %}QuizAttempt{% endblock %}

{% block body %}
    <h1>QuizAttempt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz_attempt.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ quiz_attempt.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_attempt_index') }}">back to list</a>

    <a href="{{ path('app_quiz_attempt_edit', {'id': quiz_attempt.id}) }}">edit</a>

    {{ include('quiz_attempt/_delete_form.html.twig') }}
{% endblock %}
