<form method="post" action="{{ path('app_quiz_attempt_delete', {'id': quiz_attempt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz_attempt.id) }}">
    <button class="btn">Delete</button>
</form>
