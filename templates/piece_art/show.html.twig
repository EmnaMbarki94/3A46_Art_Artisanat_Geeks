{% extends 'base.html.twig' %}

{% block title %}PieceArt{% endblock %}

{% block body %}
    <h1>PieceArt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece_art.id }}</td>
            </tr>
            <tr>
                <th>NomP</th>
                <td>{{ piece_art.nomP }}</td>
            </tr>
            <tr>
                <th>DateCrea</th>
                <td>{{ piece_art.dateCrea ? piece_art.dateCrea|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PhotoP</th>
                <td>{{ piece_art.photoP }}</td>
            </tr>
            <tr>
                <th>DescP</th>
                <td>{{ piece_art.descP }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_piece_art_index') }}">back to list</a>

    <a href="{{ path('app_piece_art_edit', {'id': piece_art.id}) }}">edit</a>

    {{ include('piece_art/_delete_form.html.twig') }}
{% endblock %}
