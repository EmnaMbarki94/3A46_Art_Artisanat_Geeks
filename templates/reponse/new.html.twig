{% extends 'baseBack.html.twig' %}

{% block title %}Créer une Réponse{% endblock %}

{% block body %}
{% block content %}
<div class="container-fluid page-body-wrapper min-vh-100">

    <!-- Sidebar -->
    <nav class="sidebar sidebar-offcanvas" id="sidebar">
        <ul class="nav">
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_admin2') }}">
                <span class="menu-title">Reclamation</span>
                <i class="mdi mdi-format-list-bulleted menu-icon"></i>
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_reponse_index') }}">
                <span class="menu-title">Reponse</span>
                <i class="mdi mdi-format-list-bulleted menu-icon"></i>
              </a>
            </li>
        </ul>
        
  </nav>

    <!-- Main Content -->
    <div class="main-panel w-100">
        <div class="content-wrapper min-vh-100 d-flex align-items-center justify-content-center">
            <div class="col-lg-10 col-md-12">
                <div class="card shadow-lg">
                    <div class="card-body">
                        <h4 class="card-title text-center mb-4">Créer une nouvelle Réponse</h4>

                        <!-- Form Section -->
                        {{ include('reponse/_form.html.twig') }}

                        <div class="text-center mt-4">
                            <a href="{{ path('app_reponse_index') }}" class="btn btn-secondary btn-lg">Retour à la liste</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>
{% endblock %}
{% endblock %}
