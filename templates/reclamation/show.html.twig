{% extends 'base.html.twig' %}

{% block title %}View Reclamation{% endblock %}

{% block body %}
 <h2 class="text-center mb-4">Reclamation Details</h2>

<section id="reclamation-view" class="p-5 d-flex justify-content-center align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card p-4">
                    <table class="table table-bordered table-striped">
                        <tbody>
                            <tr>
                                <th>Id</th>
                                <td>{{ reclamation.id }}</td>
                            </tr>
                            <tr>
                                <th>Description</th>
                                <td>{{ reclamation.descR }}</td>
                            </tr>
                            <tr>
                                <th>Date de soummission</th>
                                <td>{{ reclamation.dateR ? reclamation.dateR|date('Y-m-d') : '' }}</td>
                            </tr>
                            <tr>
                                <th>Status de la reclamation</th>
                                <td>{{ reclamation.statusR }}</td>
                            </tr>
                            <tr>
                                <th>Type de la reclamation</th>
                                <td>{{ reclamation.typeR }}</td>
                            </tr>
                        </tbody>
                    </table>

                    <div class="d-flex justify-content-center gap-3">
                        <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}" class="btn btn-warning">Edit</a>
                    </div>
                    
                    <div class="d-flex justify-content-center mt-3">
                        <!-- Delete Form -->
                        {{ include('reclamation/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}
